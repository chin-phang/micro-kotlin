apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: otel-agent-apps
  namespace: apps
spec:
  selector:
    matchLabels:
      app: otel-agent-apps
  template:
    metadata:
      labels:
        app: otel-agent-apps
    spec:
      containers:
        - name: otel-agent-apps
          image: otel/opentelemetry-collector-contrib:0.91.0
          args: ["--config=/etc/otel-agent-apps.yaml"]
          env:
            - name: K8S_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          volumeMounts:
            - name: config
              mountPath: /etc/otel-agent-apps.yaml
              subPath: otel-agent-apps.yaml
      volumes:
        - name: config
          configMap:
            name: otel-agent-apps-conf