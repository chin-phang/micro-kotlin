apiVersion: v1
kind: Secret
metadata:
  name: auth-service-credential
  namespace: apps
type: Opaque
stringData:
  REDIS_PASSWORD: password # In production, inject this via a vault/pipeline
  DB_PASSWORD: password    # In production, inject this via a vault/pipeline
